@page "/"
 
<head>
    <style>
        h1 {
            color:blue;
        }
    </style>
    
</head>
<title>TODO Manager</title>
<NavLink class="nav-link btn btn-primary" href="todoitems">TODO: Items</NavLink>
@* <NavLink class="nav-link btn btn-primary" href="compleatedtasks">Completed Items</NavLink> *@
<hr />
 <button class="btn btn-success" @onclick="OpenAboutWindow" style="float:left">Open Button Window</button>| 
 <NavLink class="btn btn-success" href="notebook">Notebook</NavLink>
 <br>
 <hr>
<NotificationsDemo></NotificationsDemo>
@code{

async Task OpenAboutWindow()
{
    string url = $"http://localhost:{BridgeSettings.WebPort}/About";

    var options = new BrowserWindowOptions
    {
        
        @* Fullscreen = true, *@
        @* AlwaysOnTop = true, *@

    };
    var mainwindow = Electron.WindowManager.BrowserWindows.First();

    var window = await Electron.WindowManager.CreateWindowAsync(options, url);
}
async Task OpenTasks()
{
    string url =$"http://localhost:{BridgeSettings.WebPort}/ToDoItems";
        var mainwindow = Electron.WindowManager.BrowserWindows.First();
            var options = new BrowserWindowOptions
    {
        @* Fullscreen = true, *@
        @* AlwaysOnTop = true, *@

    };

    var window = await Electron.WindowManager.CreateWindowAsync(options, url);

}



}